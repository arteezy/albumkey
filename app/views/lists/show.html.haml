%h2
  = @list.title

%p
  %strong Category:
  = @list.category

%p
  %strong Author:
  = link_to @list.user.email, user_path(@list.user)

= link_to 'Edit', edit_list_path(@list), class: 'btn btn-warning'
