- title 'Stats'

.col-xs-12
  .row
    .col-xs-12
      %h2
        %p.pull-left
          != 'Stats&nbsp;'
        .dropdown
          = button_tag class: 'btn btn-default dropdown-toggle', data: { toggle: 'dropdown' } do
            = params[:year] ||= 'overall'
            %span.caret
          %ul.dropdown-menu
            %li= link_to 'overall', stats_albums_path
            - Time.now.year.downto 1999 do |year|
              %li= link_to year, stats_albums_path(year)

      #chartid{ style: 'width: 100%; height: 400px; background-color: #282828;' }

  %br

  .row
    .col-md-3
      .panel.panel-default
        .panel-body
          .text-center
            %strong
              Total albums:
            = @total[:album]
    .col-md-3
      .panel.panel-default
        .panel-body
          .text-center
            %strong
              Total BNMs:
            = @total[:bnm]
    .col-md-3
      .panel.panel-default
        .panel-body
          .text-center
            %strong
              Total reissues:
            = @total[:reissue]
    .col-md-3
      .panel.panel-default
        .panel-body
          .text-center
            %strong
              Discogs data:
            = @total[:discogs]
